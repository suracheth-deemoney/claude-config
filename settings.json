{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); model=$(echo \"$input\" | jq -r '.model.display_name // \"unknown model\"'); agent=$(echo \"$input\" | jq -r '.agent.name // empty'); if [ -n \"$agent\" ]; then printf \"%s | agent: %s\" \"$model\" \"$agent\"; else printf \"%s\" \"$model\"; fi"
  }}
